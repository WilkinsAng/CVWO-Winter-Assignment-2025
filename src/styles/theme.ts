import {createTheme} from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: '#8e20d6',
        },
        secondary: {
            main: '#00bcd4', //#68d620
        }
    }
})

export default theme;